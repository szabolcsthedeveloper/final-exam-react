import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#97ce4c",
    },
    secondary: {
      main: "#1481BA",
    },
  },
  typography: {},
});

export default theme;
